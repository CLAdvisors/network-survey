import { BRAND_COLORS } from '@network-survey/frontend-shared';

export const appColors = {
  brand: BRAND_COLORS.primary,
  brandHover: BRAND_COLORS.primaryHover,
  text: BRAND_COLORS.textPrimary,
  background: BRAND_COLORS.backgroundDefault,
  surface: '#ffffff',
  border: '#e4e9e8',
};

export const appRadii = {
  small: 4,
  medium: 6,
  large: 8,
};

export const appShadows = {
  surface: '0 1px 3px rgba(28, 63, 61, 0.12)',
  raised: '0 4px 12px rgba(28, 63, 61, 0.14)',
};
