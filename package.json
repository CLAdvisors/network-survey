{
    "scripts": {
        "start-api": "cd api && set PORT=3000 && nodemon",
        "start-dashboard": "cd dashboard && set PORT=3001 && nodemon",
        "start-network-survey": "cd network-survey && set PORT=3002 && nodemon",
        "start-all": "concurrently \"npm run start-api\" \"npm run start-dashboard\" \"npm run start-network-survey\""
    },
    "devDependencies": {
        "concurrently": "^8.2.0",
        "nodemon": "^2.0.22"
    },
    "dependencies": {
        "@emotion/react": "^11.13.5",
        "@emotion/styled": "^11.13.5",
        "@mui/material": "^6.1.10",
        "@mui/x-data-grid": "^7.23.1",
        "dotenv-flow": "^4.1.0",
        "pg": "^8.11.0",
        "resend": "^0.16.0"
    }
}
